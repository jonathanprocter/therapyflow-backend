import SwiftUI

struct AIContextualHelperView: View {
    let context: AIAssistantService.AppContext

    var body: some View {
        // Placeholder overlay; replace with real implementation later
        EmptyView()
    }
}

#Preview {
    AIContextualHelperView(context: .dashboard)
}
